/**
 * Claw2Cli Plugin SDK Shim — unified module for both import paths:
 *   - require("openclaw/plugin-sdk")      (WeChat, Lark, and most plugins)
 *   - require("@openclaw/plugin-sdk")     (some plugins)
 *
 * Re-exports everything from the canonical @openclaw/plugin-sdk shim.
 */
module.exports = require("@openclaw/plugin-sdk");
